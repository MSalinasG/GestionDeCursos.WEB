@model GestionDeCursos.Data.Models.Instructor

@{
    ViewData["Title"] = "Delete";
}

<div class="card">
    <div class="card-header">
        <h4>Delete Instructor</h4>
    </div>
    <div class="card-body">
        <h3 class="alert alert-danger">Are you sure you want to delete this instructor?</h3>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(m => m.InstructorName)
            </dt>
            <dt class="col-sm-10">
                @Html.DisplayFor(m => m.InstructorName)
            </dt>

            <dt class="col-sm-2">
                @Html.DisplayNameFor(m => m.Qualification)
            </dt>
            <dt class="col-sm-10">
                @Html.DisplayFor(m => m.Qualification)
            </dt>

            <dt class="col-sm-2">
                @Html.DisplayNameFor(m => m.Experience)
            </dt>
            <dt class="col-sm-10">
                @Html.DisplayFor(m => m.Experience)
            </dt>
        </dl>
    </div>
    <div class="card-footer">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-sm btn-primary rounded-0"><i class="fas fa-trash"></i> Delete</button>
            <a asp-action="Index" class="btn btn-sm btn-primary rounded-0"><i class="fas fa-backward"></i> Back to List</a>
        </form>
    </div>
</div>